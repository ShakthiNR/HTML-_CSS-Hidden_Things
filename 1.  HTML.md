# HTML

## VSC Shortcuts
```
1. ctrl+d = to select the next selected text
2. Alt + up/down = to move text
3. select text then shift+alt+down = to copy nd paste the bunch of texts
```

## HTML
```
1. Must have one h1 tag - for SEO
2. input:reset / btn:resets --> Reset the user entered Value nd not hardcoded value
3. Use [padding,margin as 0px and box-sizing as border-box] to avoid default padding and margin
4. Id - Unique to tag but Class - Can be used in multiple tags
5. <code></code> - to write the code in the html page
6. [header,nav,main,section,article,aside,footer] - Semantic Tag (Easy Accessible)
```